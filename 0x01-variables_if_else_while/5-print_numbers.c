#include <stdio.h>
/**
 * main - prints all single digit numbers of base 10 starting from 0
 * Return: 0 (SUCCESS)
 */
int main(void)
{
	int i;

	for (i = 0; 1 < 10; i++)
		printf("%i", i);
	printf("\n");
	return (0);
}
